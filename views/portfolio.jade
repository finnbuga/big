extends layout

block content
  h1= title
  ul
    each pic in pics
      li
        picture
          source(media="(min-width: #{breakpoint})", srcset= pic[1])
          img(src= pic[0])

  include performance-tips